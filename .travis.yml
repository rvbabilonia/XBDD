language: java

jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.m2

before_install:
  - "cp .travis.settings.xml $HOME/.m2/settings.xml"

script:
  - mvn clean package
  - mvn cobertura:cobertura

after_success:
  - bash <(curl -s https://codecov.io/bash)

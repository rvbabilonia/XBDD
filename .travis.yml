language: java

jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.m2

script:
  - "cp .travis.settings.xml $HOME/.m2/settings.xml"
  - mvn clean package
  - mvn cobertura:cobertura

after_success:
  - bash <(curl -s https://codecov.io/bash)
